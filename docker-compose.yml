services:
  postgres:
    image: postgres:17-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: dx
      POSTGRES_PASSWORD: dx
      POSTGRES_DB: dx_template
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
