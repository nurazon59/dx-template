pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,jsx,tsx}"
      run: pnpm lint
    format:
      glob: "*.{js,ts,jsx,tsx}"
      run: pnpm fmt

pre-push:
  parallel: true
  commands:
    test:
      run: pnpm test
    build:
      run: pnpm build
    typecheck:
      run: pnpm typecheck
